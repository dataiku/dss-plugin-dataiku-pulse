# 2.6 Update notes

1. Patch to v2.6
1. Code-Environment does not need to be rebuilt, but helpful for Custom Recipe to go faster on DuckDB
    1. Try for 8 CPU+
1. Initialize Dashboard
1. Initialize Workers
1. Open Partitioned Folder, ensure the followig partitions
    1. layer, category, module, instance_name (no longer need date)
1. Custom Recipe now added
    1. Check that admin can be used for scenario or set the correct one (Future fix to follow workers)
    1. Manually run scenario
1. Confirm new streamlit updates, and start/run webapp
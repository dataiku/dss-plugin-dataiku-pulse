methods:
  list_dashboards:
    enabled: true
    call_mode: auto
    cleanup_mode: none
    delta_mode: auto
    artifact_key: dashboards
    notes: Preserve explicit project-level dashboard extraction for product index coverage.

  list_webapps:
    enabled: true
    call_mode: auto
    cleanup_mode: none
    delta_mode: auto
    artifact_key: webapps
    notes: Preserve explicit project-level webapp extraction for product index coverage.

  list_imported_bundles:
    enabled: false
    call_mode: auto
    cleanup_mode: none
    notes: Remains disabled until project-level handling is defined.

  list_llms:
    enabled: false
    call_mode: auto
    cleanup_mode: none
    notes: Remains disabled until project-level handling is defined.

  list_macros:
    enabled: false
    call_mode: auto
    cleanup_mode: none
    notes: Remains disabled until project-level handling is defined.

  list_ml_tasks:
    enabled: false
    call_mode: auto
    cleanup_mode: none
    notes: Remains disabled until project-level handling is defined.

  list_running_notebooks:
    enabled: false
    call_mode: auto
    cleanup_mode: none
    notes: Remains disabled until project-level handling is defined.

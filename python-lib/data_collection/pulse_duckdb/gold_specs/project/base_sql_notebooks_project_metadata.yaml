base_sql_notebooks_project_metadata:
  description: >
    Latest snapshot of sql (notebooks_project_metadata) per key.

    This table is materialized by ranking all observations by run_ts
    and keeping the newest row for each (instance_name, project_key, sql_notebooks_id).

  depends_on: []
  build_order: 10

  category: sql_notebooks
  module: project_metadata

  template: latest_by_partition
  base_table_name: base_sql_notebooks_project_metadata
  view_table_name: v_sql_notebooks__project_metadata
  partition_keys:
    - instance_name
    - project_key
    - sql_notebooks_id
